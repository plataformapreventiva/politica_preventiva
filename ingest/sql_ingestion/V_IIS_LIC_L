CREATE TABLE raw.V_IIS_LIC_L (
	"CVE_LOCC" VARCHAR(9) NOT NULL, 
	"TOT_LECHE" INTEGER NOT NULL, 
	"BENEFICIAR" INTEGER NOT NULL, 
	"FAMILIAS" INTEGER NOT NULL, 
	"TOT_LITROS" INTEGER NOT NULL, 
	"DSUBSIDIA" INTEGER NOT NULL, 
	"NIN0A12" INTEGER NOT NULL, 
	"MUJ13A15" INTEGER NOT NULL, 
	"GESTLAC" INTEGER NOT NULL, 
	"ADUL60_" INTEGER NOT NULL, 
	"ENFCRODIS" INTEGER NOT NULL, 
	"MUJ45A59" INTEGER NOT NULL, 
	"T_MAT" INTEGER NOT NULL, 
	"T_VESP" INTEGER NOT NULL, 
	"T_MIXTO" INTEGER NOT NULL, 
	"C_CONCESION" INTEGER NOT NULL, 
	"C_DICONSA" INTEGER NOT NULL, 
	"C_DISTRIB" INTEGER NOT NULL, 
	"C_SINDATO" INTEGER NOT NULL, 
	"TL_LIQUIDA" INTEGER NOT NULL, 
	"TL_POLVO" INTEGER NOT NULL, 
	"L_COMODATO" INTEGER NOT NULL, 
	"L_DICONSA" INTEGER NOT NULL, 
	"L_PARTICULAR" INTEGER NOT NULL, 
	"L_PRESTAGOB" INTEGER NOT NULL, 
	"L_PRESTADO" INTEGER NOT NULL, 
	"L_PROPIO" INTEGER NOT NULL, 
	"L_RENTADO" INTEGER NOT NULL, 
	"L_OTRO" INTEGER NOT NULL
);
